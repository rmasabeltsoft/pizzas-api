service: pizzas-api

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1

functions:
  realizarPedido:
    handler: handler.realizarPedido
    events:
      - http:
          method: post
          path: pedido
